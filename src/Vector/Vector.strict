has X Number
has Y Number
has Z Number

factory (x Number, y Number, z Number)
  return new(x: x, y: y, z: z)

factory Zero()
  return new(x: 0, y: 0, z: 0)

method CalculateDistance(target Vector) returns Number

method Times(factor Number) returns Vector

method DividedBy(divisor Number) returns Vector

method Plus(target Vector) returns Vector

method Minus(target Vector) returns Vector